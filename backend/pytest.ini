[pytest]
pythonpath = .
testpaths = app/tests
asyncio_default_fixture_loop_scope = function

# Sobreescribir DATABASE_URL para tests: usa SQLite en memoria,
# sin necesidad de tener Docker / PostgreSQL corriendo localmente.
env =
    DATABASE_URL=sqlite:///:memory:
